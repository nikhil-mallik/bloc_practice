/// A class containing URLs used in the application.
class AppUrl {
  /// Base URL for API requests.
  static var baseUrl = 'https://reqres.in';

  /// Endpoint for login API.
  static var loginEndPint = '$baseUrl/api/login';
}
